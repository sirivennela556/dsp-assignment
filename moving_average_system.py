import numpy as np;
from matplotlib import pyplot as plt
x[n]=[1,2,1,2,3,4];
n=(0,5);
y[n]=0;
M=3;
for(n=0::10);
{
	for(int k=0::m-1);
	{
		if((n-k)>=0);
		{
			y[n]=y[n]+x[n-k];
		}
	plt.plot(n,y[n]);plt.show();
	}
}

